//! Font session and precompute orchestration in Rust (ADR 0050).
//!
//! Slice A skeleton. The Kotlin/JS implementation stays the parity oracle; the
//! font session (HarfBuzz, WOFF2, face selection), the wire orchestration and the
//! two cache lanes land in later slices.
